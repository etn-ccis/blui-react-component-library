export const createPropsData = (
    prop: string,
    description: string,
    type: string,
    required: string,
    defaultValue: string
) => ({ prop, description, type, required, defaultValue });
