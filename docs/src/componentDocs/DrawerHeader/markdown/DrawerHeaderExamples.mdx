import { BasicDrawerHeader } from '../examples/BasicDrawerHeader';
import { DrawerHeaderWithSubtitle } from '../examples/DrawerHeaderWithSubtitle';
import { DrawerHeaderWithIcon } from '../examples/DrawerHeaderWithIcon';
import { DrawerHeaderWithTitleContent } from '../examples/DrawerHeaderWithTitleContent';

# Basic Drawer Header

A `<DrawerHeader />` appears at the top of a `<Drawer />` can be used to provide context to an application through a title input or a branding image.

<BasicDrawerHeader />

# Drawer Header with Subtitle

A `<DrawerHeader />` accepts a title and subtitle input.

<DrawerHeaderWithSubtitle />

# Drawer Header with Icon

A `<DrawerHeader />` accepts a blui-icon content child which can be used to manage the `<Drawer />` opened state.

<DrawerHeaderWithIcon />

# Drawer Header with Custom Content

Custom header content can be also be passed as a `titleContent` child.

<DrawerHeaderWithTitleContent />
