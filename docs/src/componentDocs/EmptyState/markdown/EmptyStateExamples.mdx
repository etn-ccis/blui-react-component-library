import { BasicEmptyState } from '../examples/BasicEmptyState';
import { EmptyStateWithDescription } from '../examples/EmptyStateWithDescription';
import { EmptyStateWithActions } from '../examples/EmptyStateWithActions';
import { EmptyStateWithContent } from '../examples/EmptyStateWithContent';
import { EmptyStateInCard } from '../examples/EmptyStateInCard';

# Basic Empty State

The `<EmptyState>` accepts a `title` and `icon` prop to create a basic placeholder.

<BasicEmptyState />

# Empty State with Description

The `<EmptyState>` accepts a `description` prop.

<EmptyStateWithDescription />

# Empty State with Actions

The `<EmptyState>` accepts an optional `actions` prop to handle events.

<EmptyStateWithActions />

# Empty State with Content Projection

The `<EmptyState>` alternatively accepts a `title` and `description` prop.

<EmptyStateWithContent />

# Empty State within a Card

The `<EmptyState>` can commonly appear within a card as a placeholder when data is not found.

<EmptyStateInCard />
