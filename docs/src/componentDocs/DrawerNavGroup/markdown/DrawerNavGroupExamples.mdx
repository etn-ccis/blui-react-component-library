import { BasicDrawerNavGroup } from '../examples/BasicDrawerNavGroup';
import { MultipleDrawerNavGroups } from '../examples/MultipleDrawerNavGroups';
import { SpacingNavGroups } from '../examples/SpacingNavGroups';
import { CustomNavGroupTitleContent } from '../examples/CustomNavGroupTitleContent';

# Basic Drawer Nav Group

A `<DrawerNavGroup />` is a wrapper used to group multiple `<DrawerNavItem />`.

<BasicDrawerNavGroup />

Multiple `<DrawerNavGroup />` can be passed into the `<DrawerBody />` to better organize navigation items.

<MultipleDrawerNavGroups />

A `<Spacer />` can be used to add space between navigation groups.

<SpacingNavGroups />

# Custom Nav Group Title Content

Custom title content can be also be passed as a `titleContent` child.

<CustomNavGroupTitleContent />
