import { SpacerWithFlex } from '../examples/SpacerWithFlex';
import { SpacerWithPixel } from '../examples/SpacerWithPixel';

# Spacer with Flex

The `<spacer>` is a utility component that adds space between elements (such as [Drawer Nav Group](/components/drawer/drawer-nav-group/examples)). The Spacer is typically used in a flex layout.

<SpacerWithFlex />

# Spacer with Pixels.

The `<spacer>` can also be given fixed dimensions instead of flex for static layouts.

<SpacerWithPixel />
