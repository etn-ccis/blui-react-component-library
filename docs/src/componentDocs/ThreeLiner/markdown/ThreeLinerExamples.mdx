import { BasicThreeLiner } from '../examples/BasicThreeLiner';
import { ThreeLinerWithCustomContent } from '../examples/ThreeLinerWithCustomContent';

# Basic Three Liner

The `<ThreeLiner>` is commonly used within a [`<AppBar>`](/components/app-bar/api-docs). It accepts a `title`, `subtitle` and `info` props.

<BasicThreeLiner />

# Three Liner with Custom Content

The `<ThreeLiner>` can alternately accept custom content for each line.

<ThreeLinerWithCustomContent />
