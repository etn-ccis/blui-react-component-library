import { BasicThreeLiner } from '../examples/BasicThreeLiner';
import { ThreeLinerWithCustomContent } from '../examples/ThreeLinerWithCustomContent';

# Basic Three Liner

The `<ThreeLiner>` is commonly used within an [`<AppBar>`](/components/app-bar/api-docs). It accepts `title`, `subtitle`, and `info` as props.

<BasicThreeLiner />

# Three Liner with Custom Content

The `<ThreeLiner>` can accept custom content for each line.

<ThreeLinerWithCustomContent />
