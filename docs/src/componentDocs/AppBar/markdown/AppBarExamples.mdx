import { CollapsedAppBar } from '../examples/CollapsedAppBar';

# App Bar

The `<AppBar>` supports multiple variants - `collapsed`, `expanded`, and `snap`.

# Collapsed Variant

A `<AppBar>` with a `collapsed` variant will stay fixed at the `collapsedHeight` size.

<CollapsedAppBar />
