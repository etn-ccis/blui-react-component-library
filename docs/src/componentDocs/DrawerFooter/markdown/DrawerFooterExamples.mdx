import { BasicDrawerFooter } from '../examples/BasicDrawerFooter';
import { ComplexDrawerFooter } from '../examples/ComplexDrawerFooter';

# Basic Drawer Footer

A `<DrawerFooter />` appears at the bottom of a `<Drawer />` can be used to display custom content.

<BasicDrawerFooter />

# Complex Drawer Footer

Any element can be passed into the `<DrawerFooter />` to display at the bottom of a `<Drawer />`.

<ComplexDrawerFooter />
