import { UserMenuWithNonTextAvatars } from '../examples/UserMenuWithNonTextAvatars';
import { UserMenuWithMenuHeader } from '../examples/UserMenuWithMenuHeader';
import { UserMenuWithCustomHeader } from '../examples/UserMenuWithCustomHeader';
import { ResponsiveUserMenu } from '../examples/ResponsiveUserMenu';
import { UserMenuWithinToolbar } from '../examples/UserMenuWithinToolbar';

## User Menu

The User Menu is a combination of an avatar and [`MUI Menu`](https://mui.com/api/menu/) that is used to hold user account-related information and actions. It is typically located in the top corner of your application within a toolbar.

<UserMenuWithinToolbar />

## Alternative Avatar Formats

The User Menu supports multiple avatar formats (text, icon, image).

<UserMenuWithNonTextAvatars />

## Adding a Menu Header

You can add a header to the top of the menu by passing `menuTitle` and `menuSubtitle` props. The avatar will also appear in the menu header.

<UserMenuWithMenuHeader />

## Customizing the Menu Header

If you want to supply your own custom menu header, you can pass in content via a `menu` prop.

<UserMenuWithCustomHeader />

## Using a Bottom Sheet

On larger screens the User Menu opens as a dropdown, but on smaller screens it opens as a bottomsheet. You can customize the point where this transition occurs via the `useBottomSheetAt` prop (default of 600px).

<ResponsiveUserMenu />
