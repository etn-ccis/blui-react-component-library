import { BasicUserMenu } from '../examples/BasicUserMenu';
import { UserMenuWithNonTextAvatars } from '../examples/UserMenuWithNonTextAvatars';
import { UserMenuWithMenuHeader } from '../examples/UserMenuWithMenuHeader';
import { UserMenuWithCustomHeader } from '../examples/UserMenuWithCustomHeader';
import { UserMenuWithPlacementOptions } from '../examples/UserMenuWithPlacementOptions';

# Basic User Menu

The `<UserMenu>` accepts an `avatar` and a `menuGroups` props.

<BasicUserMenu />

# User Menu with non-text Avatars

The `<UserMenu>` supports non-text avatars.

<UserMenuWithNonTextAvatars />

# User Menu with a Menu Header

The `<UserMenu>` can create an optional menu header through the `menuTitle` and `menuSubtitle` props.

<UserMenuWithMenuHeader />

# User Menu with a Custom Header

A custom menu header can be added by providing a `menu` prop.

<UserMenuWithCustomHeader />

# User Menu with Placement Options

The `<UserMenu>` accepts `MenuProps` to change where the overlay menu originates from.

<UserMenuWithPlacementOptions />
